services:
  - type: web
    name: proxy-scraper-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 proxy_scraper:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 